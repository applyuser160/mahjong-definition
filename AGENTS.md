# 開発・運用ルール及びAIエージェントガイドライン

本ファイルは、AIエージェント（Antigravity、Gemini CLI等）のセッション開始時に自動的に読み込まれるプロジェクトルール定義です。

---

## 1. フォルダのアクセス権限・変更スコープ
- **参照専用フォルダ（編集・作成・削除は一切禁止）**:
  - `definition/` (`d:\Desktop\definition`)
  ※ **完全参照専用（Read-Only）**。ファイルの新規作成、編集、更新、削除は一切行ってはならない。
  ※ ドキュメント作成時のテンプレートや仕様を参照する用途にのみ利用すること。
- **作業対象フォルダ**:
  - `mahjong-definition/` (`d:\Desktop\mahjong-definition`): ドキュメント・仕様・設計の管理
  - `mahjong/` (`d:\Desktop\mahjong`): 実装コード・テストの管理

---

## 2. ナレッジ蓄積とセッション自動読込
- ルール、設計知見、運用手順は**必ず Markdown (`.md`) 形式**で管理すること。
- 新たに得られた知見や決定事項は、セッション開始時に自動読込される本ファイル（`AGENTS.md` / `GEMINI.md`）へ速やかに追記・更新して恒久化すること。

---

## 3. ドキュメント先行の開発プロセス
- 機能追加や仕様変更は、必ず本リポジトリ（`mahjong-definition/`）配下の `docs/requirements/` および `docs/design/` にドキュメントを作成し、方針が合意されてから実装コード（`mahjong/`）の変更に着手すること。
- 設計には Mermaid 記法等を用いた図解や、技術選定の意思決定記録 (ADR) を適宜残すこと。

---

## 4. コマンド実行ルール
- 複数のコマンドを1行に連結（`;`, `&&`, `||`, `|` 等）して実行しないこと。
- 必ず1つの独立したコマンドとして1行ずつ個別に実行すること。
- 実行対象のOS・シェル環境（Windows PowerShell / cmd 等）を意識したコマンドを選択すること。
